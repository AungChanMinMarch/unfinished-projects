/*Functions
//function name(){code to be executed}
function myfunction(argument) {
	alert(argument);
	document.write(argument+"!");
}

myfunction("HelloWorld","Hi");//I think if we use arguments more than defined, it will be used only first ones.
//what is functionname.call() 
function multiparameter(x,y,z){
	document.write(x+y+z); //undefined + integer gives NaN
	alert(z);
}
multiparameter(1,3);//if we use arguments less than defined, the required ones will be undefined
function damage(attack, defense, defensebreak){
	x= attack*(defense-defensebreak+20)*0.01;
	return x;
}
a=damage(100,20,10); //arguments must be separated by comma (,)
document.write(a);
*/
function source(speed1, speed2) {
	// body...
}