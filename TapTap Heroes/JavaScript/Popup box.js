/*
There are three types of popup boxes:Alert, Promptt, Confirm
alert("...."); 
alert box takes one argument, which is text displayed in the box
While using alert box, the user can continue by clicking okay.
prompt("...")
the prompt method takes two parameters, the label displayed in the box, default string displayed in the text box(optional)
The prompt method prevents the user from accessing the other part of the page
var name=confirm("...");
the confirm method takes one argument, which is the text displayed in the box and returns Booleans (true or false)
The confirm method prevents the user from accessing the other part of the page until the box is closed
*/
